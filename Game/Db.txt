table Body(id)
	id uint32_t
	ptr b2Body*

table Target(id)
	id uint32_t
	target uint32_t

view BodyAndTarget
	merge-join(id) Body Target

index BodyAndTarget(target)

query Targetting
	merge-join BodyAndTarget(target) {Body(id) as TargetBody}